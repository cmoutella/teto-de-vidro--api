version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tdv-api
    container_name: TdV-api
    env_file:
      - .env
    ports:
      - '8080:8080'
    # restart: unless-stopped
